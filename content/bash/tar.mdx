---
title: Tar examples
---
import { CommandSession, Command, CommandOutput, Flag, Gloss, Kw, FileRef } from '~components/commands'

### Expand a `.tar.gz` file

<CommandSession>
<Command>ls</Command>
<CommandOutput>fruits.tar.gz</CommandOutput>
<Command>tar -<Flag kw={1}>x</Flag><Flag kw={2}>f</Flag> fruits.tar.gz</Command>
<Command>ls</Command>
<CommandOutput>apple.png banana.png canteloupe.png fruits.tar.gz</CommandOutput>
</CommandSession>

<Gloss>
<Kw kw={1}>Extracts</Kw> the contents of the <Kw kw={2}>file</Kw> <FileRef>fruits.tar.gz</FileRef> into the current directory.
</Gloss>

### Expand a `.tar` file

<CommandSession>
<Command>tar -xf fruits.tar</Command>
</CommandSession>

### Compress a directory

<CommandSession>
<Command>tar -czf fruits.tar.gz fruits/</Command>
</CommandSession>

<Gloss>
<Kw kw={2}>Compresses</Kw> the directory  <code>fruits/</code> to create the file <code>fruits.tar.gz</code>.
</Gloss>

### Compress specific files

TODO: typo? Should be x instead of v presumably?

<CommandSession>
<Command>tar -cvf fruits.tar.gz apple.png banana.png canteloupe.png</Command>
</CommandSession>

<Gloss>
Creates a compressed tarball `fruits.tar.gz` containing the three specified image files.
</Gloss>

### List the contents of a tarball

<CommandSession>
<Command>tar -tf fruits.tar.gz</Command>
<CommandOutput>{`apple.png
banana.png
canteloupe.png`}
</CommandOutput>
</CommandSession>
